# ShiHuaMusic 项目概览文档

> 整理时间：2026-08-04
> 整理目的：整体熟悉项目框架与开发方向（不含内部错误分析）

## 1. 项目类型与定位

ShiHuaMusic（诗华音乐）是一个**多平台音乐播放器客户端项目**，采用“一套产品、双端实现”的思路：

- **HarmonyOS 原生应用**（`entry/`）：基于 ArkTS + ArkUI 开发，是当前仓库的主体开发对象；
- **微信小程序**（`music-miniprogram/`）：微信原生框架 + JavaScript 开发，功能相对完整，是鸿蒙端的“参考实现”。

后端不在此仓库内，客户端通过 HTTP 对接独立后端 API 服务（开发环境指向局域网 `192.168.66.241:8080`，图片服务器 `:9000`），接口风格接近音乐类 API（banner、推荐歌单、推荐歌曲、登录注册、用户信息、歌单管理等）。

## 2. 项目结构与技术栈

### 仓库总览

| 目录/文件 | 说明 |
| --- | --- |
| `entry/` | HarmonyOS 原生应用模块（ArkTS 源码、页面、组件、工具类） |
| `music-miniprogram/` | 微信小程序端（已通过 .gitignore 排除，未纳入 Git） |
| `AppScope/` | 鸿蒙应用级配置（bundle 名、图标、应用名） |
| `hvigor/`、`oh_modules/` | HarmonyOS 构建配置与依赖 |
| `build-profile.json5`、`oh-package.json5` | 工程与依赖配置（targetSdk 6.0.0(20)） |

### 两端技术栈

- **鸿蒙端**：ArkTS、ArkUI；`@ohos.multimedia.media`（音频）、`@ohos.net.http`（网络）、`@ohos.data.preferences`（本地存储）、`@ohos.display`（深色模式检测）。
- **小程序端**：微信原生框架、JavaScript；TDesign MiniProgram UI 组件库、miniprogram-ci（构建上传工具）。

## 3. 已搭建的框架能力

两端采用了高度一致的模块化架构（pages / components / utils / types），核心框架能力包括：

1. **全局状态管理**：鸿蒙端 `GlobalDataManager`、小程序端 `globalData`，均为单例，统一管理用户信息、Token、登录状态、播放器实例与服务器地址。
2. **音频播放框架**：`AudioManager` 单例，封装播放/暂停/上下曲/进度/音量/循环模式（顺序、列表循环、单曲循环、随机），内置“发布-订阅”事件系统，供页面与底部播放器通信。
3. **网络请求层**：鸿蒙端 `HttpUtil`、小程序端 `app.request`，统一 baseUrl、自动附加 Bearer Token、401 失效处理、统一错误提示、超时与重试机制（指数退避）。
4. **主题系统**：`ThemeUtil` 提供浅色/深色/自动三种主题，颜色、间距、圆角、字号全部 token 化，页面统一取用。
5. **本地持久化**：`PreferencesUtil` / `StorageUtil`，保存用户信息、Token、欢迎弹窗标记等。
6. **页面框架**：鸿蒙端 `EntryAbility` 自定义路由表 + 底部 TabBar（`BottomNavigation`）+ 迷你播放器（`BottomPlayer`）；小程序端 tabBar + 自定义组件（player / welcome-modal / copyright）。
7. **类型定义**：`common.ets` 统一了歌手、专辑、歌曲、歌单、用户、API 响应、播放状态与播放模式等数据结构，两端共享同一套数据模型。

## 4. 功能清单与完善程度

### 小程序端（约 1.7 万行，较完整）

| 功能 | 状态 |
| --- | --- |
| 首页（轮播图 / 推荐歌单 / 推荐歌曲） | 已完成 |
| 音频播放（播放器组件、循环模式、进度、音量） | 已完成 |
| 登录 / 注册 | 已完成（表单验证、Token 持久化） |
| 用户中心 / 资料编辑（含头像） | 已完成 |
| 全部音乐 / 歌单详情 / 我的收藏 / 我的歌单 / 最近播放 | 已完成页面与数据对接 |
| 搜索 | 页面为占位文件，未注册进 app.json |

### 鸿蒙端（约 7 千行，框架就绪、业务对接进行中）

| 功能 | 状态 |
| --- | --- |
| 首页（轮播 / 推荐） | UI 与数据请求已接通（并行加载） |
| 音频播放框架 | 已移植完成；进度条拖拽、播放列表弹窗等仍为 TODO |
| 登录 / 注册 / 用户编辑 | 页面 UI 已搭建，真实 API 调用为 TODO（当前为模拟逻辑） |
| 我的歌单 / 收藏 / 最近播放 / 歌单详情 / 搜索 | 页面骨架已建，接口对接与部分跳转为 TODO |
| 底部导航 / 迷你播放器 | 已实现 |

整体判断：**小程序端基本是“完成品 / 参照物”，鸿蒙端是“正在移植中的新目标”**。鸿蒙端框架层（主题、存储、网络、音频、导航）已经成型，页面骨架齐全，但大量业务功能仍标记 TODO，尚未形成完整闭环。

## 5. 开发方向判断

从代码注释（“移植自微信小程序 …”）与两端文件结构可以明确看出开发方向：

- 以微信小程序为功能蓝本，向 HarmonyOS 原生应用做**同构移植**；
- 两端共享同一套后端 API 与数据模型；
- 当前处于“先把框架与页面铺齐、再逐个接通业务”的阶段，下一步自然是补齐鸿蒙端各页面的真实接口调用与交互闭环（登录注册、歌单详情、搜索、头像上传等）。

## 6. 版本控制现状

- 分支：`main`（存在远程 `origin/main`）；
- 提交：共 5 次，集中在 2025-11-20（初始化、播放器核心功能、资源重构等）；
- `music-miniprogram/` 在 .gitignore 中，**未纳入 Git**；
- 工作区存在未提交改动（主要为媒体资源文件重命名与相关代码更新），属于进行中的开发状态。
